# Production Environment Configuration
# DO NOT COMMIT THIS FILE TO GIT!

# JWT Configuration - CHANGE THESE!
JWT_SECRET=your-production-secret-minimum-32-characters-use-openssl-rand-base64-32
JWT_EXPIRES_IN=15m

# Database Configuration
DATABASE_URL="postgresql://prod_user:secure_password@localhost:5432/eagerdevelopers_prod?schema=public"

# CORS Configuration - CRITICAL!
# This should be your FRONTEND domain, not your API domain
CORS_ORIGIN=https://eagerdevelopers.ir

# Server Configuration
PORT=3000
NODE_ENV=production

# Redis Configuration (for your Phase 5.1 caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here

# Security Settings (increased for production)
BCRYPT_ROUNDS=12
RATE_LIMIT_TTL=60000
RATE_LIMIT_MAX=100

# Performance Settings (optimized for single core)
UV_THREADPOOL_SIZE=4

# Logging
LOG_LEVEL=warn
ENABLE_SWAGGER=false